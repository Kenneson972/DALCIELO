# Session 2026-03-24

## Géolocalisation panier (alignement DIAMANTNOIR)

- **Référence** : `DIAMANTNOIR/diamant-noir/components/VillasMapView.tsx` — `useEffect` au montage qui appelle `navigator.geolocation.getCurrentPosition` (commentaire « Détection automatique au montage »).
- **Changement DALCIELO** : au premier affichage du panier ouvert en mode **Livraison**, même appel sans attendre le clic sur le bouton cible ; reverse géocodage BAN partagé avec le bouton manuel (`fillAddressFromCoords`). Ref `deliveryAutoGeoAttemptedRef` réinitialisé quand on change de mode service (pour pouvoir relancer après passage click & collect → livraison).

**Vérification** : `npm run build` OK.

## Retrait tutoriel géoloc panier

- Demande : pas d’encart tutoriel / mentions navigateur sur la livraison.
- Supprimé : bloc « Comment activer la position », liste, note navigateurs, bandeaux granted/denied/HTTPS du bloc ; `geoPermState` + `permissions.query` + icône `Info`.
- Conservé : label adresse, champ + bouton cible, erreurs `geoError` si besoin, géoloc auto au passage en livraison + `fillAddressFromCoords`.

## Panier : erreurs validation / « problème de connexion »

- **Cause** : `handleSubmit` traitait presque tout échec HTTP comme une panne réseau (`throw` → catch → `offlineWarning`).
- **Correctif** : `submitError` avec titre + description serveur ; **403** → recharger la page ; **400** / autres 4xx → message métier sans vider le panier ; **429**, **503**, **5xx** et `fetch` en erreur → **Réessayer** ; vrai réseau → lien **secours** `localStorage` uniquement sur action utilisateur (plus d’insert Supabase anon automatique).
- **API** : messages et `code` FR sur validations `POST /api/orders` ; middleware **403** avec message FR + `CSRF_INVALID`.

**Vérification** : `npm run build` (après `rm -rf .next` si cache foireux), `tsc --noEmit`.
