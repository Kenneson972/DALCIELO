# Journal de session — 2026-03-11

## UI — Mise en page épurée (suppression mega-bloc gris/blanc)

- **type:** ui
- **summary:** Suppression du conteneur pleine page `bg-white/90 rounded-[3rem] shadow-2xl` sur menu, à propos, contact ; recherche/filtres menu dans un bandeau verre léger ; alignement customize, loading menu, pages erreur/introuvable.
- **files:** `src/components/menu/MenuPageClient.tsx`, `src/app/about/page.tsx`, `src/app/contact/page.tsx`, `src/app/customize/page.tsx`, `src/app/menu/loading.tsx`, `src/app/not-found.tsx`, `src/app/menu/[slug]/not-found.tsx`, `src/app/error.tsx`
- **why:** Demande client — rendu plus épuré, moins de bruit visuel (kb-ui-ux-pro-max).
- **impact:** Fond site plus visible ; cartes `Card` et blocs CTA conservent la structure ; lisibilité recherche/filtres via panneau discret.
- **verify:** `npm run build` OK (2026-03-11).

## Retrait NEXT_PUBLIC_ORDERING_ENABLED (commande / paiement en ligne)

- **type:** config + ui
- **summary:** Suppression du flag `NEXT_PUBLIC_ORDERING_ENABLED` et du mode `coming_soon` : plus de bandeau accueil « commande bientôt disponible », plus de modal téléphone/WhatsApp pour ce cas. `orderingBlockReason()` ne retourne plus que `monday` ou `null`. `OrderingComingSoonModal` ne gère que la fermeture du lundi.
- **files:** `src/lib/ordering.ts`, `src/components/ui/OrderingComingSoonModal.tsx`, `src/app/page.tsx`, `src/components/sections/MenuHighlight.tsx`, `src/components/sections/PizzaSlider.tsx`, `src/components/menu/PizzaCard.tsx`, `src/components/menu/AddToCartButton.tsx`, `.env.example`
- **why:** Demande client — ne plus désactiver globalement la commande (et donc le tunnel paiement) par variable d’environnement.
- **impact:** Retirer `NEXT_PUBLIC_ORDERING_ENABLED=false` de `.env.local` / Vercel si encore présent (ignoré désormais). Blocage lundi et four indisponible inchangés.
- **verify:** `npm run build` OK.

## About — lisibilité sur fond photo

- **type:** ui
- **summary:** Panneaux `readablePanel` (bordure blanche, `bg-white/82`, `backdrop-blur-md`) pour la colonne texte du hero, le bloc titre « De la farine… » et chaque carte d’étape. Couleurs texte renforcées `#2c1a12` / `opacity` pour corps.
- **files:** `src/app/about/page.tsx`
- **why:** Texte brun direct sur `#site-bg` = faible contraste.
- **impact:** Lecture confortable sans réintroduire un seul mega-bloc plein écran.
- **verify:** lint OK.

## Menu — glassmorphism cartes & filtres

- **type:** ui
- **summary:** `PizzaCard` sans `Card` blanc : conteneur `bg-white/75 backdrop-blur-md`, mobile `bg-white/93` sans blur. `CategoryFilter` pills inactives en verre léger. `MenuPageClient` : en-tête « Notre Carte » en panneau verre ; grande carte Chef en `bg-white/45` + blur (mobile quasi opaque) ; colonne texte lg avec verre ; titres de section + ombre légère ; état vide glass.
- **files:** `src/components/menu/PizzaCard.tsx`, `CategoryFilter.tsx`, `MenuPageClient.tsx`
- **why:** Demande client — moins de blocs opaques, style glass tout en restant lisible sur `#site-bg`.
- **impact:** Fond tropical visible à travers les cartes ; aligné règle perf (blur désactivé &lt; md sur ces utilitaires).
- **verify:** `npm run build` OK.

## Home CTA Pizza du Chef + pill nav desktop

- **type:** ui
- **summary:** `MenuHighlight` : ordre **Détails** puis **Commander l’exclu**. `Header` : `LayoutGroup`, seul `motion.span` avec `layoutId="headerActiveNavPill"` (plus de `bg-[#D4633F]` sur le lien actif), barre `min-h-[52px]`, padding scroll `py-2.5` vs `py-4` pour limiter le saut vertical au scroll.
- **files:** `src/components/sections/MenuHighlight.tsx`, `src/components/layout/Header.tsx`
- **verify:** lint (pas de build demandé session).

## Header pill — mesure DOM (plus de layoutId)

- **summary:** Indicateur orange : un seul `motion.div` animé en `left` / `top` / `width` / `height` depuis `getBoundingClientRect` du lien actif vs conteneur ; `useLayoutEffect` + double `requestAnimationFrame` ; `resize` + dépendance `scrolled`. Plus de `layoutId` / `LayoutGroup`.
- **why:** Après scroll + navigation, l’animation layout partait d’une fausse origine (effet « sort par le bas »).
- **verify:** lint.

## Retrait mentions d'une personne (branding)

- **type:** ui+docs+script
- **summary:** Remplacement de toutes les mentions d'une personne par “equipe de Dal Cielo” (UI textes, docs, workflow n8n, commentaires SQL et récap projet/session).
- **files:** `src/app/about/page.tsx`, `src/components/sections/AboutSection.tsx`, `src/data/menuData.ts`, `src/components/layout/CartDrawer.tsx`, `src/components/order/PendingValidationView.tsx`, `src/components/order/FAQAccordion.tsx`, `src/components/order/OrderTrackingClient.tsx`, `src/app/admin/page.tsx`, `src/app/api/orders/queue-estimate/route.ts`, `n8n/CieloBot - Pizza dal Cielo (1).json`, `docs/AUDIT_TUNNEL_VENTE_WHATSAPP.md`, `docs/RECEPTE_PDF_N8N.md`, `docs/RECAP_SESSION_RECU_PANIER.md`, `RECAP_SESSION.md`, `RECAP_PROJET.md`, `.env.example`, `database/mysql/004_create_stocks.sql`, `database/supabase/014_add_order_receipt_category.sql`, `database/mysql/README.md`
- **why:** Demande client — cohérence branding et suppression de la référence à une personne.
- **impact:** Tous les messages clients et le prompt CieloBot utilisent le même référentiel “equipe”.
- **verify:** `rg` : aucun résultat.

## Accueil — retrait « Pourquoi Dal Cielo » + galerie → Instagram seul

- **type:** ui
- **summary:** Sur `/` : suppression du bloc `USPSection` (3 piliers). `GallerySection` ne rend plus la grille d’images Unsplash ni le double header ; uniquement la carte CTA vers Instagram (`contactInfo.socials.instagram`).
- **files:** `src/app/page.tsx`, `src/components/sections/GallerySection.tsx`
- **why:** Demande client — alléger l’index et ne pas afficher de photos placeholder.
- **impact:** Parcours Hero → Pizza du Chef → (slider) → Instagram → Contact. Composant `USPSection.tsx` non importé mais toujours dans le repo.
- **verify:** `npm run build` OK.

## SEO — titre unique & marque « Pizza Dal Cielo »

- **type:** seo
- **summary:** Cause du double nom dans Google : `metadata.title.template` du layout (`%s | Pizza …`) fusionné avec un `title` page qui contenait déjà « Pizza … ». **Accueil** : `title.absolute` pour un seul titre complet. **Pages produit** : `title` = nom du produit seul (suffixe ajouté par le layout). Remplacement global `Pizza dal Cielo` → **`Pizza Dal Cielo`** dans `src/`, `contactInfo.name`, README, workflow n8n (prompts/messages).
- **files:** `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/menu/[slug]/page.tsx`, ensemble de `src/**`, `README.md`, `n8n/CieloBot - Pizza dal Cielo (1).json`
- **why:** Demande client — SERP et cohérence cap D sur « Dal ».
- **impact:** Footer, schémas JSON-LD, textes client alignés ; Google peut mettre plusieurs jours à rafraîchir l’aperçu (Search Console : inspection d’URL).
- **verify:** `npm run build` OK ; aucune occurrence `Pizza dal Cielo` dans `src/`.

## Documentation — guide dashboard admin

- **type:** docs
- **summary:** Création de `docs/GUIDE_DASHBOARD_ADMIN.md` : URL `/admin`, auth `ADMIN_PIN` / rate limit / CSRF, toutes les vues (dashboard KPI, homepage slider & desserts, four & queue-settings, commandes & validate Stripe, reçus, cuisine, menu & upload, popups, avis, stocks, analytics), flux métier et dépannage.
- **files:** `docs/GUIDE_DASHBOARD_ADMIN.md`
- **why:** Demande client — guide simple d’emploi sans masquer les détails techniques (API, Stripe, Supabase).
- **impact:** Référence unique pour formation et support.
- **verify:** Aligné avec `src/app/admin/page.tsx`, `src/lib/adminAuth.ts`, routes `src/app/api/admin/*`.

## Documentation — guide admin simplifié

- **type:** docs
- **summary:** Réécriture de `GUIDE_DASHBOARD_ADMIN.md` : langage non technique, table des sections, **Menu & Produits** détaillé (modifier prix, descriptions, photos, dispo, ajout article, Pizza du Chef), flux commandes en phrases simples ; techno reléguée à une courte fin « pour l’équipe technique ».
- **files:** `docs/GUIDE_DASHBOARD_ADMIN.md`
- **why:** Demande client — guide trop technique.
- **impact:** Lecture côté restaurant sans jargon API / Stripe / Supabase dans le corps du texte.
- **verify:** relecture.

## Documentation — récap projet complet (non technique)

- **type:** docs
- **summary:** Ajout de `docs/RECAP_PROJET_COMPLET.md` : contexte Pizza Dal Cielo / Karibloom, objectifs par public, pages client, parcours commande, résumé admin + lien guide, image de marque, mentions légales, table des docs du repo, phrase de synthèse.
- **files:** `docs/RECAP_PROJET_COMPLET.md`
- **why:** Demande client — récap complet sans jargon technique.
- **impact:** Document partageable avec le restaurant ou des partenaires.
- **verify:** relecture.

## Fix admin — liste commandes vs Supabase (fantômes)

- **type:** fix + docs
- **summary:** Dashboard n’utilise plus `localStorage` comme repli silencieux ; messages si réponse API invalide ; synchro `pdc_orders` après chargement OK ; suppression champ debug `_dbg` sur `GET /api/admin/orders` ; `docs/DEPANNAGE_DASHBOARD_COMMANDES.md` (hypothèses : 2 projets Supabase, cache, non-JSON, HTTP 401/403/429/503, RLS…).
- **files:** `src/app/admin/page.tsx`, `src/app/api/admin/orders/route.ts`, `docs/DEPANNAGE_DASHBOARD_COMMANDES.md`
- **why:** Incohérence 11 commandes en base vs 16 à l’écran avec pending obsolètes.
- **impact:** Vérité terrain = API/Supabase ; navigateur aligné après sync réussie.
- **verify:** `npm run build` OK.

## Fix ordersStore — faux « pending » (statut base)

- **type:** fix
- **summary:** `rowToOrder` mappait tout statut hors liste sur `pending_validation`. Désormais chaîne vide / inconnu → `completed` (affichage) ; `getOrders` déduplique par `id` et ignore id vide ; doc SQL pour trouver les lignes anormales.
- **files:** `src/lib/ordersStore.ts`, `docs/DEPANNAGE_DASHBOARD_COMMANDES.md`
- **why:** 5 commandes toujours « à valider » alors que `WHERE status = 'pending_validation'` vide.
- **verify:** `npm run build` OK.

## SEO — nom de site Google (Pizza Dal Cielo vs domaine)

- **type:** seo
- **summary:** Renforcement signaux « site name » : `applicationName`, `appleWebApp`, `manifest.ts`, JSON-LD `Organization` + `WebSite` (`alternateName`, `publisher`, `about`) ; doc `docs/SEO_NOM_DE_SITE_GOOGLE.md`.
- **files:** `src/app/layout.tsx`, `src/app/manifest.ts`, `src/components/seo/JsonLd.tsx`, `docs/SEO_NOM_DE_SITE_GOOGLE.md`
- **why:** SERP affichait `pizzadalcielo.com` comme nom au lieu de la marque.
- **verify:** `npm run build` OK ; Rich Results Test recommandé post-déploiement.

## UI — bandeau fermeture & texte dimanche / lundi

- **type:** ui
- **summary:** Message dimanche : fermé dim. **et** lun., commande à partir de **mardi** (18h–22h). Bandeau rouge retiré du dessus du site ; nouveau `ClosedDayOrderingBanner` sous le Hero (fond léger, texte brun). Modal commande alignée.
- **files:** `src/components/ui/ClosedDayOrderingBanner.tsx`, `src/app/page.tsx`, `src/components/ui/OrderingComingSoonModal.tsx`, `src/lib/ordering.ts`
- **verify:** `npm run build` OK.

## UI — Dashboard : options pizza / étiquettes (modales produit)

- **type:** ui
- **summary:** Refonte des zones « toggles + badges » dans `ProductCreateModal` et `ProductEditModal` : composants `AdminProductOptionTogglesSection` (grille) et `AdminBadgeLabelsSection` (grille + ligne d’ajout pleine largeur) ; texte d’aide ; « Aucune étiquette sur la carte » au lieu d’un chip ambigu avec X ; option booléenne végétarienne renommée « Recette végétarienne ». En édition pizza, « Afficher dans le slider d’accueil » est sous la section image slider.
- **files:** `src/components/admin/MenuManager.tsx`
- **why:** Mise en page irrégulière (flex-wrap), séparateur vertical arbitraire, doublon sémantique Végétarien.
- **impact:** Formulaires admin plus lisibles ; comportement des champs inchangé côté API.
- **verify:** `npm run build` OK ; prévisualisation `/admin` → Menu & produits → créer / modifier une pizza.

## UI — Dashboard menu : drag-and-drop photos produits

- **type:** ui
- **summary:** Composant `AdminImageDropzone` (idle / accept / reject, Lucide, Tailwind) branché sur les modales **Nouveau produit** et **Modifier** (galerie ou photo unique) et sur la zone **Slider page d’accueil** (variante `compact`). Upload via `uploadProductImage` / `uploadSliderImage` vers `/api/admin/upload` comme avant.
- **files:** `src/components/admin/ui/AdminImageDropzone.tsx`, `src/components/admin/MenuManager.tsx`
- **why:** UX proche d’un Dropzone Mantine sans ajouter Mantine.
- **impact:** Glisser-déposer + bouton « Parcourir les fichiers » ; JPEG/PNG/WebP/GIF, max 5 Mo.
- **verify:** `npm run build` OK.
